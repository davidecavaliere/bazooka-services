name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: install
      run: yarn install --frozen-lockfile
    - name: test
      run: yarn test
    - name: build
      run: yarn build
